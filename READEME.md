zuerst muss noch nodejs installiert werden unter https://nodejs.org/en/download dort am besten v24,
wird für npm benötigt

ide war webstorm

```cd IMMA-frontend```

um in IMMA-Frontend/IMMA-frontend zu landen

dort dann: ```npm install```

das installiert alle notwendigen packages

wenn alles durchgelaufen ist, ```npm run dev```
das startet den frontend server mit dem chat zu imma

in der konsole steht dann auch die adresse worunter man die website findet
dort kann man dann mit imma chatten, aber nur 20 anfragen für text pro tag
und man kan einen text und ein bild hochladen, das kostet uns aber geld/tokens

